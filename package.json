{
  "name": "lumixia-web",
  "version": "1.0.0",
  "private": true,
  "description": "Lumixia — pronájem LED obrazovek a LED posterů",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/prepare-preview.mjs",
    "check": "node --check script.js && node --check scripts/dev.mjs && node --check scripts/prepare-preview.mjs && node --check pixel-guide.js && node --check hero-video.js && node --check scripts/prepare-wedos.mjs && node --test tests/frontend.test.mjs",
    "build:wedos": "node scripts/prepare-wedos.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
