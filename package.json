{
  "name": "lumixia-web",
  "version": "1.0.0",
  "private": true,
  "description": "Lumixia — LED obrazovky a postery",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/prepare-preview.mjs",
    "check": "node --check script.js && node --check scripts/dev.mjs && node --check scripts/prepare-preview.mjs && node --check led-display.js"
  },
  "engines": {
    "node": ">=20"
  }
}
